import React from 'react';
import PropTypes from 'prop-types';

Artist.propTypes = {
    
};

function Artist(props) {
    return (
        <div>
            artist
        </div>
    );
}

export default Artist;