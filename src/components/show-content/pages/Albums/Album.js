import React from 'react';
import PropTypes from 'prop-types';

Album.propTypes = {
    
};

function Album(props) {
    return (
        <div>
            album
        </div>
    );
}

export default Album;